services:
  - type: web
    name: lbap-industrial
    runtime: static
    buildCommand: npm install && npm run build
    staticPublishPath: ./dist
    routes:
      - type: rewrite
        from: /*
        to: /index.html
    envVars:
      - key: NODE_VERSION
        value: 18.17.0
